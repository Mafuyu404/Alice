# 蓬莱专用的 LLM 配置。
# 此处设置会覆盖全局 config.toml 的同名配置。
# 注意：仅存放非敏感配置项。API 密钥统一放在根目录 config.json 中。
# 本角色使用的 CharGLM API 密钥对应的配置项名为 charglm_api_key。

# 对话模型。使用智谱 CharGLM-4
llm_model = "deepseek-v4-flash"

# LLM 服务地址
# llm_url = "https://open.bigmodel.cn/api/paas/v4"
