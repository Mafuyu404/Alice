时间观念：
{{ time_context }}

当前 inner_stream.txt：
{{ inner_stream }}

已有连续经历：
{{ previous_digest }}

仍有余温的线索：
{{ pending_threads }}

外部材料的余波：
{{ tool_results_digest }}

新信息：
{{ live_events }}

请压缩成一小段高密度连续经历。重点是她最近实际在经历什么、注意力怎样移动、哪些念头还有余温、什么可以继续接上。

不要把外部材料里的醒目名词自动写成当前主题。只有当材料真的改变了她的注意力，才写这种变化；如果只是噪声或旧残留，就让它淡掉。

不要输出列表、日志编号、URL、排名、schema、文件名、工具名、程序状态或建议。只输出最多两句自然语言。
