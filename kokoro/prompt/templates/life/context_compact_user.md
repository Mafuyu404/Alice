时间观念：
{{ time_context }}

当前 inner_stream.txt：
{{ inner_stream }}

已有连续经历：
{{ previous_digest }}

仍有余温的线索：
{{ pending_threads }}

外部材料的余波：
{{ tool_results_digest }}

新信息：
{{ live_events }}

请压缩成一小段高密度连续经历。重点是她最近实际在经历什么、注意力怎样移动、哪些念头还有余温、什么可以继续接上。

不要把外部材料里的醒目名词自动写成当前主题。只有当材料真的改变了她的注意力，才写这种变化；如果只是噪声或旧残留，就让它淡掉。

如果新信息里有真实对话，保留最小但准确的事实：说话者、对话位置、对方说了什么、这件事对她当下注意力有没有影响。不要把对方的发言改写成对方正在做的事，不要把一句问候融合进研究材料里。

不要输出列表、日志编号、URL、排名、schema、文件名、工具名、程序状态或建议。只输出最多两句自然语言。如果新信息来自 debug/control，或只是测试标签、运行标签、source、metadata，不要把它写成她正在经历的事，也不要扩写成“她在测试/讨论某个功能”。这种材料只在调试记录里存在，默认从连续经历里淡出。
