id = "say"
name = "say"
actions = ["say", "say_precomputed", "wait", "stay_silent"]
description = "Speak outward when expression naturally wants to leave the inner stream, use a precomputed utterance, or intentionally remain silent/wait to digest. Silence is a valid living action, but it is not a substitute for a clear external research question."
needs_prepare_llm = true
needs_after_llm = true
prepare_prompt = "prepare.md"
after_prompt = "after.md"
