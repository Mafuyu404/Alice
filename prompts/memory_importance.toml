# Prompt module: memory_importance
# Entry prefix: memory_importance.*
# Loaded by kokoro.core.prompts; keep dotted keys stable because code references them directly.

[memory_importance]
user_template = '''
判断以下对话是否包含值得长期记忆的有用信息。

值得记忆的内容包括：用户偏好、兴趣、个人背景、目标、重要事件、具体需求、习惯、观点、关系、日期。
不值得记忆的内容包括：日常问候、简短确认、无实质内容的闲聊、重复信息、纯语气词。

{user_name}：{user_msg}
{name}：{assistant_msg}

请只回答"重要"或"不重要"。
'''
