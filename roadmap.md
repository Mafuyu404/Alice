# Alice Chat Framework Roadmap

本文档从“流畅对话体验”角度整理后续优化路线。重点不只是提升单个模型能力，而是降低端到端延迟、稳定轮次状态、改善语音打断、控制主动发言边界，并统一 CLI 与 WebUI 的对话行为。

## 目标

- 文字聊天首 token 尽量控制在 1.5 秒内。
- 语音聊天从用户说完到听到首句尽量控制在 2.5 秒内。
- TTS 支持自然打断，避免用户需要等待角色完整说完。
- 主动发言更少打扰，更像自然延续，而不是随机插话。
- CLI 与 WebUI 使用同一套上下文、角色提示、记忆注入规则。
- 记忆只保存有长期价值的信息，并能去重、更新和避免误记。

## P0：立即处理

### 1. 核对并修复编码问题

当前部分源码、配置、提示词和前端文案在终端读取时呈现乱码。需要确认是控制台编码问题，还是文件内容本身已经损坏。

优先核对文件：

- `kokoro/prompts.py`
- `kokoro/character.py`
- `kokoro/screen_interest.py`
- `kokoro/memory.py`
- `config.toml`
- `index.html`

验收标准：

- 所有中文提示词、正则标点、隐私关键词、UI 文案以 UTF-8 正常显示。
- STT 分句正则能识别中文 `。！？；` 和英文 `.?!;`。
- 隐私关键词和记忆重要性判断不再出现乱码匹配项。

### 2. 统一端到端延迟埋点

现有 CLI 已输出部分延迟信息，但还不能完整定位慢点。需要增加统一的 trace id 和阶段耗时。

建议记录：

- `speech_end -> refined_text`
- `refine_start -> refine_done`
- `llm_request_start -> llm_first_token`
- `llm_first_token -> llm_done`
- `tts_sentence_flush -> tts_first_audio`
- `speech_end -> first_audio`
- `speech_end -> tts_done`

验收标准：

- 每轮对话能看到完整耗时链路。
- 可区分慢在 STT、精修、LLM、TTS 还是播放缓冲。

### 3. 补全 TTS 停止能力

`kokoro/tts_minimax.py` 中 `stop_playback()` 目前为空，需要实现真实停止。

建议行为：

- 停止当前播放流。
- 清空待播放音频队列。
- 标记当前 fetch worker 尽快退出。
- 丢弃已经缓冲但未播放的句子。
- 保持后续 `prepare()` 能重新启动播放线程。

验收标准：

- 用户打断时，当前语音能在 300ms 到 800ms 内停止。
- 停止后下一轮 TTS 可以正常播放。

## P1：明显提升流畅度

### 4. 优化 STT 提交策略

当前 `kokoro/pool.py` 等待文本稳定后再精修。这个策略稳，但部分短句会显得慢。

建议改进：

- 短句、确认句、寒暄句直接提交，跳过 LLM 精修。
- 长句或明显存在 ASR 异常时再调用精修模型。
- 根据静音、标点、语气词动态缩短 `stable_seconds`。
- 支持“快速提交 + 后台修正”模式，先响应用户，再把精修结果用于历史或记忆。

验收标准：

- “嗯”“好”“继续”“你看一下这个”这类短句不再额外等待精修。
- 长句仍能通过精修减少识别错误。

### 5. 改进 TTS 分句和首句策略

当前 TTS 按字符数和标点刷新，建议增加首句快速播报策略。

建议规则：

- 首句更短，8 到 14 个字即可触发合成。
- 后续句子保持 20 到 35 字或遇到句末标点再触发。
- “好”“我看看”“等一下”这类反馈句立即播放。
- 对代码、列表、长解释禁用逐句 TTS 或改为摘要播报。

验收标准：

- LLM 流式输出开始后，TTS 首音频更快出现。
- 长回复不会被切成过碎的语音片段。

### 6. 增加通用等待回应

当前用户命令分支已有等待回应，建议扩展到所有可能超过 1.5 秒的操作。

适用场景：

- 屏幕分析。
- 长问题推理。
- 记忆检索。
- 外部命令执行。
- 模型服务首 token 迟迟未返回。

验收标准：

- 用户发起慢任务后能很快听到短反馈。
- 等待回应不进入长期记忆。
- 等待回应不会污染正式 assistant 回复历史。

## P2：对话状态重构

### 7. 抽象 ConversationRuntimeState

当前状态分散在 `chat_lock`、`tts_engine.is_playing`、主动发言调度和屏幕监控线程里。建议抽出统一状态机。

建议状态：

- `LISTENING`
- `USER_SPEAKING`
- `REFINING`
- `THINKING`
- `SPEAKING`
- `INTERRUPTED`
- `BACKGROUND_ANALYZING`
- `ERROR_RECOVERY`

需要统一消费状态的模块：

- `cli.py`
- `kokoro/pool.py`
- `kokoro/proactive.py`
- `kokoro/screen_interest.py`
- `kokoro/memory_events.py`
- TTS backend

验收标准：

- 主动发言不会在用户说话、TTS 播放、模型生成中抢占。
- 屏幕监控和记忆事件能根据状态自动延迟。
- 用户打断能明确进入 `INTERRUPTED` 并取消后续输出。

### 8. 支持三档语音交互模式

建议新增配置：

```toml
voice_interaction_mode = "safe"
```

可选值：

- `safe`: TTS 播放时暂停 STT，适合外放。
- `barge_in`: TTS 播放时仍监听，检测到用户说话后停止 TTS。
- `headset`: TTS 和 STT 同时运行，适合耳机或硬件回声消除。

验收标准：

- 不同硬件环境可以选择合适策略。
- `barge_in` 模式下用户能自然插话。

## P3：上下文与记忆质量

### 9. 上下文装配分层限流

当前 `kokoro/chat_session.py` 已有 system、screen、memory、history 的基础结构。建议增加明确预算。

推荐优先级：

1. 角色设定：常驻，但保持简短。
2. 当前任务上下文：例如用户刚要求看屏幕。
3. 最近对话：保留最近 6 到 12 轮。
4. 相关记忆：只注入 3 到 5 条强相关记忆。
5. 屏幕上下文：只注入最新 1 到 2 条，并带过期时间。
6. 主动发言 guidance：只在主动发言轮次加入。

验收标准：

- 长时间聊天后上下文不会无限膨胀。
- 记忆和屏幕上下文不会压过用户当前请求。
- 主动发言提示不会污染普通对话。

### 10. 结构化记忆写入

当前记忆后端已有重要性过滤，后续可升级成事件抽取。

建议记忆类型：

- 用户偏好。
- 用户计划。
- 重要日期。
- 人物关系。
- 长期目标。
- 禁忌与不喜欢的内容。
- 近期任务。

建议字段：

- `type`
- `content`
- `confidence`
- `source_turn`
- `created_at`
- `updated_at`
- `expires_at`

验收标准：

- 不保存纯寒暄和无长期价值内容。
- 同类记忆能合并、去重、更新。
- 用户纠正时能覆盖旧记忆。
- AI 猜测不会被当作事实保存。

## P4：主动发言与屏幕感知

### 11. 加强主动发言边界

`kokoro/proactive.py` 已经有 desire、disturbance、cooldown 和 diversity。建议继续增强打扰感控制。

建议规则：

- 用户连续高频输入时冻结主动发言。
- 用户短答、否定、取消、沉默拒绝后进入安静期。
- 主动发言默认只说一句。
- 主动发言必须有明确来源：最近对话、屏幕、日期、记忆或空闲。
- 同一来源重复触发需要更长冷却。

验收标准：

- 主动发言不会连续追问。
- 用户不接话时，系统会自动降低主动性。
- 主动发言更像自然延续，而不是定时广播。

### 12. 屏幕上下文过期与变化检测

当前屏幕兴趣会注入 session，并触发 SCREEN desire。建议加入过期机制和前台窗口变化检测。

建议规则：

- 屏幕上下文默认 2 到 5 分钟过期。
- 前台窗口变化后旧上下文降权。
- 用户主动要求“看屏幕”时，结果优先级高于周期性 screen watch。
- 隐私窗口命中后进入临时 quiet period。

验收标准：

- 不评论已经过期的屏幕内容。
- 隐私场景不触发主动发言。
- 用户主动命令能取消后台屏幕分析。

## P5：WebUI 与 CLI 一致性

### 13. WebUI 使用服务端 ChatSession

当前 WebUI 前端自己构造 system prompt，CLI 使用 `ChatSession`。这会导致两个入口行为不一致。

建议改造：

- 前端只发送 `character_id`、`session_id` 和用户文本。
- 服务端维护每个 session 的 `ChatSession`。
- 后端统一调用 `build_messages()`。
- 前端不再保存完整 system prompt。
- 记忆注入、角色提示、历史裁剪都由服务端完成。

验收标准：

- 同一角色在 CLI 和 WebUI 中说话风格一致。
- WebUI 支持后端记忆和统一历史裁剪。
- 模型切换不会破坏会话上下文。

### 14. 前端交互修复与完善

建议检查 `index.html` 中模型加载、TTS badge、文案和错误展示。

重点：

- `loadModels()` 当前提前 `return`，模型下拉不会填充。
- `checkTTS()` 当前也提前 `return`，TTS 状态不会展示。
- 流式错误需要更清晰展示。
- 支持停止生成。
- 支持重新生成上一轮。
- 支持清空当前会话。

验收标准：

- WebUI 模型下拉可用。
- 用户能停止正在流式生成的回复。
- 网络错误、模型错误、记忆错误有清晰提示。

## 错误恢复策略

所有模块应尽量降级，而不是打断主对话。

- LLM 不可用：给用户可理解的简短错误。
- TTS 失败：继续文字回复。
- STT 精修失败：使用原始 STT 文本。
- 记忆失败：静默降级，不影响回复。
- 屏幕分析失败：不主动告知，除非用户明确要求看屏幕。
- 主动发言失败：记录日志，不影响主会话。

## 推荐实施顺序

### 第一阶段：低风险快速收益

- 修复或确认编码问题。
- 完整延迟埋点。
- 实现 TTS stop。
- 优化短句 STT 精修跳过。
- 优化 TTS 分句和首句策略。

### 第二阶段：语音自然度

- 引入统一运行状态机。
- 实现 `safe` / `barge_in` / `headset` 三档语音模式。
- 泛化等待回应。
- 主动发言增加安静期和用户反馈降权。

### 第三阶段：长期对话质量

- 上下文预算。
- 结构化记忆。
- 屏幕上下文过期。
- WebUI 与 CLI 统一后端会话。

### 第四阶段：产品化体验

- WebUI 停止生成、重新生成、清空会话。
- 更清晰的错误恢复。
- 对话质量日志与调参面板。
- 主动发言反馈按钮或语音反馈命令。

## 成功标准

- 用户短句能快速得到回应，不明显等待系统“想完”。
- 用户可以自然打断角色语音。
- 角色不会在用户忙碌、刚拒绝、正在说话时主动插话。
- 长聊后仍能保持稳定人设和相关记忆。
- CLI 与 WebUI 行为一致。
- 失败时能优雅降级，不破坏当前会话。
